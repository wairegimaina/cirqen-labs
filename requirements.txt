# =============================================================
#  Cirqen Labs – Full Requirements
#  Generated by scanning all *.py files in the project
#  Updated: Django 4.2 → 5.2 LTS (Python 3.14 compatible)
# =============================================================

# ── Core Framework ────────────────────────────────────────────
Django>=5.2.8,<5.3

# ── Async / ASGI / WebSockets ─────────────────────────────────
# Bumped floors: older channels/daphne releases predate Django 5.2
# support — verify your WebSocket consumers still work after upgrade.
channels>=4.1
channels-redis>=4.2
daphne>=4.1

# ── Task Queue (Celery + Beat) ────────────────────────────────
celery>=5.4
django-celery-beat>=2.7
redis>=5.0

# ── Database ──────────────────────────────────────────────────
psycopg2-binary>=2.9

# ── Cache (Redis backend for Django) ─────────────────────────
django-redis>=5.4

# ── REST / API ────────────────────────────────────────────────
djangorestframework>=3.15
django-cors-headers>=4.4
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0
httpx>=0.27.0
python-multipart>=0.0.9
requests>=2.31
aiohttp>=3.9
flask>=3.0

# ── PDF Generation ────────────────────────────────────────────
reportlab>=4.1

# ── Excel / Spreadsheets ──────────────────────────────────────
openpyxl>=3.1

# ── Word Documents ────────────────────────────────────────────
python-docx>=1.1
docxtpl>=0.16

# ── Data Science / Plotting ───────────────────────────────────
numpy>=1.26
pandas>=2.1
matplotlib>=3.8
plotly>=5.18
dash>=2.16
# django-plotly-dash has historically lagged new Django releases —
# test the dash/plotly admin views manually after upgrading.
django-plotly-dash>=2.4

# ── Image Processing ──────────────────────────────────────────
Pillow>=10.2
qrcode>=7.4

# ── Date / Time Utilities ────────────────────────────────────
python-dateutil>=2.8
pytz>=2024.1

# ── Cryptography & Security ───────────────────────────────────
cryptography>=42.0

# ── Messaging / Streaming ────────────────────────────────────
kafka-python>=2.0

# ── Monitoring / System ───────────────────────────────────────
psutil>=5.9

# ── Networking / Graph ────────────────────────────────────────
networkx>=3.2

# ── Streaming Archives ────────────────────────────────────────
zipstream-new>=1.1.8

# ── Environment / Config ──────────────────────────────────────
python-dotenv>=1.0
appdirs>=1.4

# ── GUI (Desktop)
PySide6>=6.6

# ── Dev / Debug ───────────────────────────────────────────────
django-debug-toolbar>=4.4
django-extensions>=3.2
pytest>=8.0
pytest-django>=4.9
chartjs

# ── Scheduling (required by calSchedules app — see calSchedules/tests.py) ──
django-scheduler>=2.0
