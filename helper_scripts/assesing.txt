# View
cat ~/.cirqen/data/config.json | python3 -m json.tool

# Edit inline (update hq_db in one shot)
clearYES
import json; from pathlib import Path
p = Path.home() / '.cirqen/data/config.json'
c = json.loads(p.read_text())
c['hq_db'].update({
    'host': 'dpg-d8fj2c59j78s738al2vg-a.ohio-postgres.render.com',
    'database': 'cirqen_hq_db1',
    'user': 'cirqen_hq_db1_user',
    'password': 'cTAU3kJL3NNlUYA9rR07kh87FKHA6c24'
})
p.write_text(json.dumps(c, indent=2))
print('Done')
"
python3 manage.py migrate --database=hq
